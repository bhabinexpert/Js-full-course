// Object Methods..

const obj = {
    name: "Bhabin",
    age: 20,
    city:"Damak"

}

const entries = Object.entries(obj)
console.log(entries) // prints the array of key value pair inside an array

const keys = Object.keys(obj)
console.log(keys) // returns the keys in an array

const values = Object.values(obj)
console.log(values)

const num ={
    x:1,
    y:5,
    z: 10
}

const values2 = Object.values(num);
console.log(values2)


